apiVersion: v1
kind: Service
metadata:
  name: bore
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "bore"
    port: 8080
  selector:
    app: bore
