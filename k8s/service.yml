apiVersion: v1
kind: Service
metadata:
  name: yoit
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "yoit"
    port: 8080
  selector:
    app: yoit
