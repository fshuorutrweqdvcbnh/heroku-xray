apiVersion: v1
kind: Service
metadata:
  name: head
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "head"
    port: 8080
  selector:
    app: head
